# Tracked stub for Docker Compose `env_file` — always present after clone (no missing-file error).
# Optional DATABASE_URL / REDIS_URL: use project-root `.env` next to docker-compose.yml (Compose interpolation).
COMPOSE_ENV_STUB=1
HOST=0.0.0.0
