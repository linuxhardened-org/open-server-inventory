services:
  db:
    image: postgres:15-alpine
    container_name: servervault-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: servervault
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      # Host 3001 avoids conflict when host:3000 is already used (e.g. Grafana)
      - "3001:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DATABASE=servervault
      - SESSION_SECRET=servervault-docker-session-secret-min-32-chars
    depends_on:
      - db
    volumes:
      - server-data:/app/data

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    ports:
      # Host 8081 avoids conflict when host:8080 is already in use
      - "8081:80"
    depends_on:
      - server

volumes:
  postgres-data:
  server-data:
